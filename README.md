# Topology_Map_Project

## Python 
Parsing the JSON schema into a diagram map of the cloud environment.

## JSON Schema
A schema that will hopefully be able to describe the entire environment and its changes.